# Dataset folder

Place the original dataset here with this filename:

`Sales Marketing.csv`

The CSV is excluded from Git by default to avoid publishing customer data. The deployed Streamlit app only needs the trained model artifact in `model/churn_model.joblib`.
